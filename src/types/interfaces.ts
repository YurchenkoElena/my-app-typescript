export interface ITodoType {
    title: string
    id: number
    completed: boolean
}


export interface ICoffeeCardType {
    id: number,
    title: string,
    price: string,
    description: string,
    country: string,
    image: string
}